K = 10
N = { __ref__ = ["fit.toml", "validation_dataset", "neurons", "N"] }
space_extent = { __ref__ = ["fit.toml", "validation_dataset", "neurons", "space_extent"] }
window = { __ref__ = ["fit.toml", "validation_dataset", "abs_window"] }
# window = [750.0, 500.0]
# perturbations_extent = [505.0, 379.5]                                                      # spreadout
perturbations_extent = [292.5, 258.0]                        # compact
kappas = { Untuned = 0.0, Cotuned = 2.0 }
pipeline = { __ref__ = ["fit.toml", "validation_pipeline"] }

[state_dict]
__call__ = ["torch", "load"]
weights_only = true
map_location = "cpu"

[state_dict.f]
__call__ = ["niarb.io", "iterdir"]
path = "fits"
pattern = "*.pt"
indices = { __call__ = ["builtins", "int", { __environ__ = "INDEX" }] }
